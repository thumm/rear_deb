version scheme
  * For packages based on git development commit
        <last released version>+1~<abbreviated commit hash>-<debian revision>
    with
        <abbreviated commit hash> == git log -n 1 --format="%h"
